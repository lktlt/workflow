import { CanOperateDirective } from './can-operate.directive';

describe('DiretivesDirective', () => {
  it('should create an instance', () => {
    const directive = new CanOperateDirective();
    expect(directive).toBeTruthy();
  });
});
